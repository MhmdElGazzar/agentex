# Node
node_modules/

# Playwright CLI scratch
.playwright-cli/

# QA run artifacts — keep the folder + its README, ignore actual run outputs
executions/*
!executions/README.md

# Secrets
.env
.env.*
!.env.example

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Consumer-project conventions scaffolded by test-design into the project
# under test — the plugin ships the blank template, not a filled-in copy
.agentex/

# OS / editor
.DS_Store
Thumbs.db
